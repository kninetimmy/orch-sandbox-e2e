# Orch configuration (PRD §17). Generated by `orch init`; change it through
# `orch configure`, not by hand — canonical configuration changes go through
# a Delivery PR.

schema_version  = 1
config_revision = "sha256:af51b0b2e492"

[concurrency]
max_subagents = 3

[merge]
strategy = "squash"

[memhub]
mode = "off"

[metrics]
enabled = false

[hosts.codex.roles.architect]
model  = "gpt-5.6-sol"
effort = "high"

[hosts.codex.roles.scout]
model  = "gpt-5.6-terra"
effort = "low"

[hosts.codex.roles.implementer]
model  = "gpt-5.6-terra"
effort = "high"

[hosts.codex.roles.specialist]
model  = "gpt-5.6-sol"
effort = "medium"

[hosts.codex.roles.reviewer]
model  = "gpt-5.6-sol"
effort = "medium"

[hosts.codex.roles.review_downgrade]
model  = "gpt-5.6-terra"
effort = "high"
